<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_margin="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_user"
            android:layout_marginEnd="8dp"/>

        <TextView
            android:id="@+id/testimonialName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Anna"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#000000" />

        <RatingBar
            android:id="@+id/testimonialRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:numStars="5"
            android:rating="5"
            android:isIndicator="true"
            android:stepSize="1.0"
            android:progressDrawable="@drawable/rating_bar_selector"
            style="?android:attr/ratingBarStyleSmall"/>
    </LinearLayout>

    <TextView
        android:id="@+id/testimonialFeedback"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SignLingo has completely changed the way I learn sign language. It's very user-friendly and the scan feature is very helpful!"
        android:textSize="14sp"
        android:textColor="#000000"
        android:layout_marginTop="4dp" />
</LinearLayout>
